% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/commands.R
\name{dockerfile}
\alias{dockerfile}
\title{Generate Dockerfile lines}
\usage{
dockerfile(x, platform = NULL, missing_only = TRUE, ...)
}
\arguments{
\item{x}{A \code{sysreqr_plan} or package vector.}

\item{platform}{Platform specification accepted by \code{\link[=resolve_platform]{resolve_platform()}}.}

\item{missing_only}{Whether to include only packages not known to be
installed.}

\item{...}{Passed to \code{\link[=check_packages]{check_packages()}} when \code{x} is not a plan.}
}
\value{
A single Dockerfile snippet.
}
\description{
Produces a Dockerfile \code{RUN} snippet that installs the system packages a
plan needs. On \code{apt} platforms the output uses the standard
\verb{apt-get update && apt-get install -y --no-install-recommends ... && rm -rf /var/lib/apt/lists/*} pattern.
}
\examples{
plan <- check_packages("xml2", platform = "ubuntu-22.04")
cat(dockerfile(plan))
}
\seealso{
Other commands:
\code{\link[=admin_request]{admin_request()}},
\code{\link[=github_actions]{github_actions()}},
\code{\link[=install_command]{install_command()}}
}
\concept{commands}
