% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/setup-advice.R
\name{setup_advice}
\alias{setup_advice}
\title{Get beginner setup advice for R package installation on Linux}
\usage{
setup_advice(
  packages = NULL,
  platform = NULL,
  backend = c("auto", "bundled", "ppm", "pak"),
  repo = "cran",
  check_installed = TRUE,
  include_r_project_repo = TRUE,
  script = NULL
)
}
\arguments{
\item{packages}{Optional R package names to check.}

\item{platform}{Platform specification accepted by \code{\link[=resolve_platform]{resolve_platform()}}.}

\item{backend}{One of \code{"auto"}, \code{"bundled"}, \code{"ppm"}, or \code{"pak"}.}

\item{repo}{Repository name used for Posit Package Manager and sysreq lookup.}

\item{check_installed}{Whether to check installed system packages on the
current host when possible.}

\item{include_r_project_repo}{Whether to include optional R Project operating
system repository commands for supported Linux distributions.}

\item{script}{Optional path. When supplied, an executable shell script is
written with the safe setup commands.}
}
\value{
A \code{sysreqr_setup_advice} object.
}
\description{
Produces a practical checklist for users who are new to R on GNU/Linux. It
recommends binary package repositories where available, explains when R
Project operating system repositories are relevant, lists source build
tools, and can add package-specific system requirements. It never runs
\code{sudo}, edits the system R configuration, or changes operating system
repositories; the user remains in control.
}
\examples{
advice <- setup_advice(
  "xml2",
  platform = "ubuntu-22.04",
  backend = "bundled"
)
print(advice)

\donttest{
# Write a reviewable shell script:
setup_advice(
  c("xml2", "curl"),
  platform = "ubuntu-22.04",
  backend = "bundled",
  script = tempfile(fileext = ".sh")
)
}
}
\seealso{
Other setup:
\code{\link[=explain]{explain()}},
\code{\link[=print.sysreqr_setup_advice]{print.sysreqr_setup_advice()}}
}
\concept{setup}
