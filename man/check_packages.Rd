% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/check.R
\name{check_packages}
\alias{check_packages}
\title{Check system requirements for R packages}
\usage{
check_packages(
  packages,
  platform = NULL,
  backend = c("auto", "bundled", "ppm", "pak"),
  repo = "cran",
  dependencies = NA,
  upgrade = TRUE,
  check_installed = TRUE
)
}
\arguments{
\item{packages}{Package names or package references.}

\item{platform}{Platform specification accepted by \code{\link[=resolve_platform]{resolve_platform()}}.}

\item{backend}{One of \code{"auto"}, \code{"bundled"}, \code{"ppm"}, or \code{"pak"}.}

\item{repo}{Repository name used in notes and by the PPM backend.}

\item{dependencies}{Dependency policy passed to \code{pak::pkg_sysreqs()} when the
pak backend is used.}

\item{upgrade}{Upgrade policy passed to \code{pak::pkg_sysreqs()} when the pak
backend is used.}

\item{check_installed}{Whether to check installed system packages on the
current host when possible.}
}
\value{
A \code{sysreqr_plan}.
}
\description{
Resolves the system packages an R package needs on a given Linux platform
and returns them in a structured plan. The \code{"auto"} backend prefers the
offline bundled database on \code{apt} platforms, then Posit Package Manager,
then \code{pak}.
}
\examples{
plan <- check_packages(c("xml2", "curl"), platform = "ubuntu-22.04")
plan

install_command(plan)

\dontrun{
# Network-dependent: queries Posit Package Manager.
check_packages("xml2", platform = "ubuntu-22.04", backend = "ppm")
}
}
\seealso{
\code{\link[=check_project]{check_project()}}, \code{\link[=check_library]{check_library()}}, \code{\link[=diagnose_log]{diagnose_log()}}.

Other preflight:
\code{\link[=check_library]{check_library()}},
\code{\link[=check_project]{check_project()}},
\code{\link[=detect_project_packages]{detect_project_packages()}}
}
\concept{preflight}
